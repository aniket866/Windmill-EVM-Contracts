# Deployment account private key (hex, without 0x prefix).
PRIVATE_KEY=

# RPC endpoints used by scripts and optional verification flows.
MAINNET_RPC_URL=
SEPOLIA_RPC_URL=

# Block explorer API key used by `forge verify-contract` and `forge script --verify`.
ETHERSCAN_API_KEY=
